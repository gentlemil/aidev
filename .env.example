# Database — SQLite for local dev, swap provider + URL for PostgreSQL in production
DATABASE_URL="file:./dev.db"

# Auth.js — generate a secret with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here"

# App URL
NEXTAUTH_URL="http://localhost:3000"

# API keys and configuration for AI providers
OPENAI_API_KEY=
OPENROUTER_API_KEY=
AI_PROVIDER=

# Custom configuration for AI Devs Hub
AI_DEVS_KEY=
AI_DEVS_VERIFY_URL=

# SERVER CONFIG
SERVER_PORT_SSH=
SERVER_LOGIN=
SERVER_URL=
SERVER_PASSWORD=

# run on Mac with command: ssh SERVER_LOGIN@SERVER_URL -p SERVER_PORT_SSH