/**
 * AI layer — reserved for future Claude / AI SDK integration.
 *
 * Planned exports:
 *   export { anthropicClient } from "./anthropic"
 *   export { runAgent }        from "./runner"
 *   export type { AgentRunOptions, AgentRunResult } from "./types"
 *
 * Each feature module (features/agents, features/documents) will
 * import from here to run AI workflows.
 */

export {}
